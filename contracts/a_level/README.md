# A-level contracts

These are sanitized copies of the frozen A1--A7 run contracts and formally recorded amendments. They preserve estimands, data partitions, seeds, numerical policies, statistical boundaries, and execution gates without exposing local paths, identities, logs, checkpoints, or raw molecular records.

The contracts document the executed analyses. They are not manuscript text and do not imply that excluded inputs or weights are distributed in this package.
